#include <iostream>
#include "List.h"
using namespace std;

int main()
{
    setlocale(LC_ALL, "ru");
    srand(time(NULL));

    //Matrix matrix;
    //matrix.MakeMatrix();
    //Order line;
    //line.MakeArr();
    //Matrix* Array2 = &line;
    //Array2->Show();

    Node <int> node(5);
    List <int> list;
    //list.Add(5);
    //list.Add(6);
    return 0;
}